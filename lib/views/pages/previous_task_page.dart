import 'package:flutter/material.dart';

class PreviousTaskPage extends StatelessWidget {
  static const routeName = '/previous-task';
  const PreviousTaskPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
